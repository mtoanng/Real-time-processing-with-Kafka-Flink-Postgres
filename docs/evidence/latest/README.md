# Latest deployment evidence

Status: **NOT VERIFIED**.

This directory is reserved for small, sanitized proof from the latest real
deployment. Evidence must include offline `user_features_1m_canonical` and the
corresponding latest Redis feature hash. Follow `docs/RUNBOOK.md` section 12. Keep the complete evidence
archive outside Git and commit only selected JSON/text summaries and focused
screenshots. Never commit the source dataset, `.env`, credentials, account
identifiers, public addresses or large runtime logs.
